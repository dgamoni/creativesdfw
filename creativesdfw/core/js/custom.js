jQuery(document).ready(function($) {
	  
	  $('.bxslider').bxSlider({
	    slideWidth: 400,
	    minSlides: 2,
	    maxSlides: 3,
	    slideMargin: 10,
	     pager: false
	  });

	  // $('.image-link').magnificPopup({
	  // 	  type:'image',
  	// 	  gallery:{enabled:true},
	  // });

});